model PublicHolidayCalendar {
  id       String @id @default(uuid())
  year     Int
  holidays String // JSON array of holidays

  workCenterId String
  workCenter   WorkCenter @relation(fields: [workCenterId], references: [id], onDelete: Cascade)

  @@unique([workCenterId, year])
}
