model Sprint {
  id        String @id @default(uuid())
  name      String
  startDate String
  endDate   String

  projectId String
  project   Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
}
